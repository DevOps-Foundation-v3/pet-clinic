name: Java CI 

on: [push]
 jobs:
  build:

    runs-on: ubuntu-latest

    steps:
  
    - name: Chechout
      uses: actions/checkout@v3.5.3
     
    - name: Descarga Codigo
      run: ls -l

    - name: hola mundo
      run: echo "hola mundo"
